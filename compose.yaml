services:
  app:
    build:
      context: .
      target: dev
    tty: true
    user: "${UID:-1000}:${GID:-1000}"
    stdin_open: true
    volumes:
      - .:/app:cached
      - bundler:/bundle
      - vscode:/home/app/.vscode-server

volumes:
  vscode:
  bundler:
